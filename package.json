{
  "name": "association-dev",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "predeploy": "echo 'predeploy'",
    "deploy": "gh-pages -d ."
  },
  "devDependencies": {
    "json-server": "^0.17.0",
    "gh-pages": "^5.0.0"
  }
} 